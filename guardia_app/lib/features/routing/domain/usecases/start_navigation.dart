import 'package:guardia_app/features/routing/domain/entities/route_option_entity.dart';

class StartNavigation {
  Future<void> call(RouteOptionEntity route) async {
    // Optional Backend call to register active navigation
    // Implementation can be empty if handled purely in client state for now
  }
}
